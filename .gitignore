# Credenciais (NUNCA versionar) — contem app_secret do OMIE
config*.json
!config.example.json

# Banco local sincronizado (dados reais + hashes de senha)
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.pyc

# Sondas/temporarios
_probe*.py

# Variaveis de deploy (contem segredos)
*.env
